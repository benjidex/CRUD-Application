# TODO - Make TaskMaster run

- [x] Create `server/package.json` and move backend into `server/index.js` (Express REST API on :4000)
- [x] Create `client/package.json`, `vite.config.js`, `index.html`, and React entry in `client/src/`
- [x] Copy React components into `client/src/` and wire imports
- [ ] Run backend: `cd server && npm install && npm start`
- [ ] Run frontend: `cd client && npm install && npm run dev`
- [ ] Verify in browser: http://localhost:3000

